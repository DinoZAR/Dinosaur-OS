#! /bin/python

# This program will create a brand new RAWR filesystem as a binary file. It must
# be appended to the MBR later to create a valid boot disk

rawrImage = open("rawrImage.img")


# Create the Block Allocation Bitmap. This is used to easily find an empty block
# with which to use for storing my files. It must have at least enough bits to
# make up the entire system. Finding the size of the volume is a whole another
# thing, so here we just say how big it is, in largest LBA.

sizeLBA = 2560000







rawrImage.close()
